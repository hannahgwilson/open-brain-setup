# Supabase CLI local state (project refs, pooler URL — regenerated by `supabase link`)
supabase/.temp/

# Secrets — never commit
.env
.env.*
*.key

# Personal — local-only, contains real names/paths
CLAUDE.md

# OS
.DS_Store

# Deno
*.orig
